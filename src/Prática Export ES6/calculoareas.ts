import * as Areas from './Areas';

let lado = 5;
let base = 20;
let altura = 2;

console.log(`A área de um quadrado, com lado ${lado} é igual a ${Areas.calculoAreaQuadrado(lado)}`);
console.log(`A área de um triangulo, com base ${base} e altura ${altura} é igual a ${Areas.calculoAreaTriangulo(base, altura)}`);