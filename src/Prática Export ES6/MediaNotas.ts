export function MediaTresNotas(nota1: number, nota2: number, nota3: number):number {
    return (nota1 + nota2 + nota3) / 3;
}

export function MediaQuatroNotas(n1:number, n2: number, n3: number, n4: number): number {
    return (n1 + n2 + n3 + n4) / 4;
} 